<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Invoice <%= invoice._id %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css" rel="stylesheet" />
  <style>
    :root {
      --primary: #3b82f6;
      --primary-dark: #1e40af;
      --primary-darker: #1e3a8a;
      --secondary: #0ea5e9;
      --accent: #06b6d4;
      --dark-bg: #0f172a;
      --dark-card: #1e293b;
      --dark-card-hover: #334155;
      --text-light: #f1f5f9;
      --text-muted: #94a3b8;
      --success: #10b981;
      --warning: #f59e0b;
      --error: #ef4444;
      --gradient: linear-gradient(90deg, var(--primary), var(--secondary));
      --gradient-dark: linear-gradient(90deg, var(--primary-dark), var(--primary-darker));
    }
    
    a {
      text-decoration: none;
    }
    body {
      background-color: var(--dark-bg);
      font-family: "Inter", sans-serif;
      color: var(--text-light);
    }
    .invoice-box {
      max-width: 800px;
      margin: 40px auto;
      background: var(--dark-card);
      border-radius: 12px;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.3);
      padding: 40px;
      position: relative;
      overflow: hidden;
      border: 1px solid rgba(255, 255, 255, 0.1);
    }
    .invoice-box::before {
      content: "";
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 4px;
      background: var(--gradient);
    }
    .invoice-header {
      border-bottom: 2px solid var(--primary);
      padding-bottom: 10px;
      margin-bottom: 25px;
    }
    .company-name {
      font-size: 1.4rem;
      font-weight: 600;
      color: var(--primary);
    }
    .invoice-title {
      text-align: right;
      font-weight: 700;
      font-size: 1.6rem;
      color: var(--text-light);
    }
    .details-table {
      color: var(--text-light);
      border-color: rgba(255, 255, 255, 0.1) !important;
    }
    .details-table td {
      padding: 8px 4px;
      border-color: rgba(255, 255, 255, 0.1) !important;
      background-color: var(--dark-card-hover) !important;
      color: var(--text-light);
    }
    .details-table th {
      background-color: var(--dark-card-hover) !important;
      color: var(--text-light);
      border-color: rgba(255, 255, 255, 0.1) !important;
    }
    .summary {
      border-top: 2px solid rgba(255, 255, 255, 0.1);
      padding-top: 10px;
    }
    .status {
      font-weight: 600;
      color: <%= invoice.status === "Paid" ? "var(--success)" : "var(--error)" %>;
    }
    .btn-print {
      margin-top: 25px;
    }
    .invoice-number {
      background: rgba(255, 255, 255, 0.1);
      padding: 5px 10px;
      border-radius: 4px;
      font-weight: 600;
      color: var(--text-light);
    }
    .payment-status {
      display: inline-block;
      padding: 5px 15px;
      border-radius: 20px;
      font-weight: 600;
      font-size: 0.9rem;
    }
    .paid-status {
      background-color: rgba(16, 185, 129, 0.2);
      color: var(--success);
      border: 1px solid rgba(16, 185, 129, 0.3);
    }
    .unpaid-status {
      background-color: rgba(239, 68, 68, 0.2);
      color: var(--error);
      border: 1px solid rgba(239, 68, 68, 0.3);
    }
    .total-amount {
      font-size: 1.2rem;
      color: var(--success);
    }
    .bg-light {
      background-color: var(--dark-card-hover) !important;
      border-color: rgba(255, 255, 255, 0.1) !important;
      color: var(--text-light);
    }
    .text-primary {
      color: var(--primary) !important;
    }
    .text-muted {
      color: var(--text-muted) !important;
    }
    .btn-outline-primary {
      color: var(--primary);
      border-color: var(--primary);
    }
    .btn-outline-primary:hover {
      background-color: var(--primary);
      color: var(--text-light);
    }
    .btn-danger {
      background-color: var(--error);
      border-color: var(--error);
    }
    .btn-danger:hover {
      background-color: rgba(239, 68, 68, 0.8);
      border-color: rgba(239, 68, 68, 0.8);
    }
    
    /* Print styles */
    @media print {
      :root {
        --dark-bg: white; 
        --dark-card: white; 
        --text-light: #000000; 
        --dark-card-hover: #f8f9fa;
      }
      
      body {
        background-color: white;
        font-size: 12pt;
        color: black;
      }
      .invoice-box {
        box-shadow: none;
        margin: 0;
        padding: 20px;
        max-width: 100%;
        border: 1px solid #ccc;
      }
      .no-print {
        display: none !important;
      }
      .btn-print, .btn-success, .btn-outline-secondary, .btn-danger {
        display: none !important;
      }
      .invoice-box::before {
        height: 3px;
      }
      .bg-light {
        background-color: #f8f9fa !important;
      }
      .details-table {
        color: black;
      }
      .details-table th {
        background-color: #f8f9fa !important;
        color: black;
      }
    }
  </style>
</head>

<body>
  <div class="invoice-box">
    <div class="d-flex justify-content-between invoice-header">
      <div>
        <div class="company-name text-uppercase">Service: <%= invoice.task_id?.title || "Service Title" %></div>
        <small class="text-muted"><%= invoice.Worker_id %></small><br />
        <small class="text-muted">Date Issued: <%= new Date(invoice.createdAt).toLocaleDateString('en-US', {
  weekday: 'short',
  year: 'numeric',
  month: 'short',
  day: '2-digit'
}) %>
</small>
      </div>
      <div class="invoice-title">
        INVOICE
        <div class="invoice-number mt-1">#<%= invoice._id.toString().slice(-6).toUpperCase() %></div>
      </div>
    </div>

    <div class="row mb-4">
      <div class="col-md-6">
        <h6 class="text-primary">Bill To:</h6>
        <div class="border rounded p-3 bg-light">
          <p class="mb-1"><b><%= invoice.user_id.username || "Customer Name" %></b></p>
          <p class="mb-0 text-muted"><%= invoice.user_id.email %></p>
        </div>
      </div>
      <div class="col-md-6 text-end">
        <h6 class="text-primary">Service Info:</h6>
        <div class="border rounded p-3 bg-light">
          <p class="mb-1">Service: <b><%= invoice.task_id.title%></b></p>
          <p class="mb-0">
            Status: 
            <span class="payment-status <%= invoice.status === "Paid" ? "paid-status" : "unpaid-status" %>">
              <%= invoice.status %>
            </span>
          </p>
        </div>
      </div>
    </div>

    <table class="table table-bordered details-table text-light">
      <thead class="">
        <tr>
          <th>Description</th>
          <th class="text-end">Amount (₹)</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td>Service Charge</td>
          <td class="text-end">₹<%= invoice.total_amount.toFixed(2) %></td>
        </tr>
        <tr>
          <td>Platform Fee (10%)</td>
          <td class="text-end">₹<%= invoice.platform_fee.toFixed(2) %></td>
        </tr>
      </tbody>
      <tfoot>
        <tr class="summary">
          <td><b>Total</b></td>
          <td class="text-end total-amount"><b>₹<%= invoice.total_amount.toFixed(2) %></b></td>
        </tr>
      </tfoot>
    </table>

    <div class="text-center no-print">
      <% if (invoice.status === "unpaid") { %>
        <div class="mt-4">
          <span class="payment-status unpaid-status p-2">
            <i class="bi bi-exclamation-circle"></i> Pending Payment
          </span>
        </div>
      <% } else { %>
        <div class="mt-4">
          <span class="payment-status paid-status p-2">
            <i class="bi bi-check-circle"></i> Payment Completed
          </span>
        </div>
      <% } %>
    </div>

    <div class="text-center no-print">
      <button class="btn btn-outline-primary btn-print" onclick="window.print()">
        <i class="bi bi-printer"></i> Print / Download PDF
      </button>
    </div>
    
    <div class="text-center mt-3 no-print">
      <button class="btn btn-danger mx-auto">
        <a href="/user/payments/invoice" class="text-white">
          <i class="bi bi-house"></i> Back to Invoices
        </a>
      </button>
    </div>
  </div>
</body>
</html>